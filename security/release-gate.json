{
  "schema_version": 1,
  "updated_at": "2026-07-11",
  "release_target": "controlled production beta",
  "confirmed_launch_decisions": {
    "product_scope": "web application only",
    "business_name": "Showcase",
    "public_founder": "Kumar Dyutit",
    "market": "English-speaking worldwide with US-focused marketing and no geo-restriction",
    "launch_mode": "closed beta",
    "starting_invites_per_day": 10,
    "ai_budget_usd": {
      "daily": 5,
      "monthly": 100
    },
    "founding_offer": {
      "spots": 10,
      "price_usd_per_year": 99,
      "enable_after": "Stripe test-mode verification passes"
    },
    "email": {
      "support_and_privacy": "hello@tryshowcase.ink",
      "inbound_forward_to": "hello@tryshowcase.ink",
      "weekly_scorecard": "kumar.dyutit09@gmail.com",
      "postal_address_status": "unresolved",
      "required_delivery_state": "disabled"
    },
    "distribution": {
      "x_account": "@Showcase_app1",
      "linkedin": "not available; not a launch channel",
      "buffer": "approved for drafts",
      "first_partner_wave_size": 5,
      "partner_drafts_require_approval_before_send": true
    }
  },
  "verification": {
    "evidence_base_git_head": "97e9bd796ce56fa03b0ac9544a89d6d5415799d4",
    "source_fingerprint_sha256": "5788990e71f5c62028c3288a9f3f110f8a4a638391b70aa124dd9313aa77fc42",
    "verified_at": "2026-07-12T03:39:29Z"
  },
  "contracts": {
    "required_requirement_ids": [
      "GROWTH-CODE",
      "GROWTH-MIGRATIONS",
      "GROWTH-PROD-CONFIG",
      "GROWTH-PROD-DEPLOY",
      "GROWTH-PROVIDER-E2E",
      "GROWTH-DISTRIBUTION-APPROVAL",
      "P0-RACE",
      "P1-GLOBAL-CEILING",
      "P0-01",
      "P0-02",
      "P0-08",
      "P0-RLS",
      "P1-01",
      "P1-13",
      "P1-16",
      "P1-19"
    ],
    "growth_migrations": [
      "20260710033035_referral_publish_credit.sql",
      "20260710033036_pending_parses.sql",
      "20260710033037_referral_completion_credit.sql",
      "20260710033038_growth_admission.sql",
      "20260710033039_growth_automation.sql",
      "20260710033040_proofscore_public_capacity.sql",
      "20260710033041_launch_security_and_webhooks.sql",
      "20260710033042_founding_member_slots.sql",
      "20260710033043_email_and_parse_hardening.sql",
      "20260710033044_atomic_subscription_state.sql",
      "20260710033045_referral_invite_pacing.sql",
      "20260710033046_referral_abuse_and_credit_hardening.sql",
      "20260710033047_explicit_data_api_grants.sql",
      "20260712035035_retire_public_proofscore_infrastructure.sql"
    ],
    "crons": {
      "/api/cron/interview-retention": "0 5 * * *",
      "/api/cron/data-retention": "45 4 * * *",
      "/api/cron/invite-batch": "0 15 * * *",
      "/api/cron/lifecycle-email": "0 13 * * *",
      "/api/cron/weekly-digest": "0 14 * * 1",
      "/api/cron/growth-scorecard": "30 14 * * 1"
    },
    "required_production_env_names": [
      "NEXT_PUBLIC_SUPABASE_URL",
      "NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY",
      "SUPABASE_SERVICE_ROLE_KEY",
      "DATABASE_URL",
      "STRIPE_SECRET_KEY",
      "NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY",
      "STRIPE_WEBHOOK_SECRET",
      "STRIPE_PRICE_ID_PRO_MONTHLY",
      "STRIPE_PRICE_ID_PRO_ANNUAL",
      "STRIPE_PRICE_ID_FOUNDING_ANNUAL",
      "OPENAI_API_KEY",
      "OPENAI_COST_RATES_JSON",
      "OPENAI_GENERAL_DAILY_BUDGET_USD",
      "OPENAI_GENERAL_MONTHLY_BUDGET_USD",
      "AI_GLOBAL_DAILY_LIMIT",
      "INTERVIEW_GLOBAL_DAILY_BUDGET_USD",
      "INTERVIEW_GLOBAL_MONTHLY_BUDGET_USD",
      "INTERVIEW_KILL_SWITCH",
      "KILL_SWITCH_AI",
      "KILL_SWITCH_GEMINI",
      "KILL_SWITCH_CHECKOUT",
      "KILL_SWITCH_JOBS_PROVIDER",
      "KILL_SWITCH_PUBLISHING",
      "NEXT_PUBLIC_APP_URL",
      "CRON_SECRET",
      "RESEND_API_KEY",
      "RESEND_WEBHOOK_SECRET",
      "INBOUND_FORWARD_TO",
      "RESEND_DELIVERY_WEBHOOK_SECRET",
      "UNSUBSCRIBE_SIGNING_SECRET",
      "RESEND_FROM_EMAIL",
      "EMAIL_POSTAL_ADDRESS",
      "EMAILS_ENABLED",
      "LIFECYCLE_EMAILS_ENABLED",
      "GROWTH_SCORECARD_EMAIL",
      "ABUSE_IP_HASH_SALT",
      "LAUNCH_OPEN"
    ],
    "optional_production_env_names": [
      "ERROR_WEBHOOK_URL",
      "INVITE_EXCLUDE",
      "BUFFER_API_KEY",
      "BUFFER_ORGANIZATION_ID",
      "FOUNDER_NAME"
    ]
  },
  "requirements": [
    {
      "id": "GROWTH-CODE",
      "area": "growth",
      "description": "Paced acquisition, activation, referral, monetization, retention, and measurement code passes the offline release suite",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "2026-07-11 verification for revision 130d12a retired the anonymous public score funnel while preserving authenticated audit data and the Evidence Audit engine. npm run verify passed 60 repository checks, TypeScript, lint with zero errors, and the optimized 108-page/route production build. npm run test:launch-offline passed growth automation, admission, the new proofscore-retirement regression, Founding, referral pacing, 100 deterministic AI-budget checks, distribution, database grants, launch security, backup verification, and marketing truthfulness. The retirement regression proves /proofscore redirects to the canonical landing page, anonymous score/reserve/stash code is absent, active distribution assets cannot restore the retired route, and the authenticated audit engine remains. Axe reported zero issues across all nine public pages; git diff --check passed. Unaliased production and canonical-domain probes matched before and after promotion. Follow-up source cleanup removed the retired stash test from the disposable local-Supabase harness and removed the three retired public APIs from the authorization matrices; the harness-safety and retirement regressions pass. On 2026-07-12 at 02:46Z, revision 97e9bd7 was re-verified after purging the obsolete public ProofScore marketing packet: the complete launch-offline suite passed, npm run verify again passed 60 static checks, TypeScript, lint with zero errors, and the optimized 108-page/route build, and git diff --check remained clean. On 2026-07-12, the prepared final infrastructure-retirement tree removed the claim route and capacity helper, introduced the generic fail-closed 32-character abuse-salt boundary, added guarded migration 048, and reconciled the authorization inventory to 78 route files and 97 handlers. The complete launch-offline suite passed; npm run verify passed 60 static checks, TypeScript, lint with zero errors, and an optimized 107-route build; local harness safety and git diff checks passed. Database application and credentialed post-drop proofs remain correctly blocked behind the migration time/data gates and separate production rollout requirements."
    },
    {
      "id": "GROWTH-MIGRATIONS",
      "area": "database",
      "description": "The canonical migration history resets cleanly and the controlled production promotion is recoverable and verified",
      "status": "BLOCKED",
      "release_blocking": true,
      "evidence": "security/production-rollout-evidence.json records the authorized 2026-07-10 maintenance window. Fresh backup 20260710T180210Z passed authenticated database-plus-Storage restore verification against 2,008 rows immediately before the first write. Production history-only repair was limited to equivalent migrations 033-037; the pinned CLI dry-run then showed exactly 038-047 and that reviewed batch applied successfully. Read-only verification proved the canonical 48-record ledger through 047, unchanged row counts for all 67 pre-existing objects, unchanged 77/61 Auth/Storage ledgers, 13 expected new tables, paused controls, one Auth signup trigger, nine Storage policies, and zero RLS/ACL/index/constraint/trigger/admission/attribution/referral/webhook invariant violations.",
      "blocker": "Migration 048 is prepared but not applied. After 2026-07-12 03:50:34+00, verify the time and active-data guards on an isolated local reset, stage and smoke-test the backward-compatible build unaliased, promote that exact build to canonical and probe it, then require the production dry-run to show only 20260712035035. Apply it in the authorized window and record read-only proof of the 49-record ledger plus the three retired RPC/table removals."
    },
    {
      "id": "GROWTH-PROD-CONFIG",
      "area": "deployment",
      "description": "Every production variable and provider boundary required by the growth system is configured",
      "status": "BLOCKED",
      "release_blocking": true,
      "evidence": "Production configuration was re-audited on 2026-07-10 under the prior 37-name contract. Twenty-six launch, email, AI, Gemini, checkout, jobs, publishing, Interview, and budget values were explicitly written and read back in their fail-closed state; AI_GLOBAL_DAILY_LIMIT was temporarily 1. The prior ProofScore-specific salt and unsubscribe secret were generated without disclosure. The current contract retires that product-specific variable and requires ABUSE_IP_HASH_SALT instead. On 2026-07-11, a fresh 64-character hexadecimal value was generated without disclosure and added to the canonical showcase-app Production environment as a Sensitive value; `vercel env ls production` read-back confirmed its encrypted presence. EMAIL_POSTAL_ADDRESS, the provider-issued RESEND_DELIVERY_WEBHOOK_SECRET, and STRIPE_PRICE_ID_FOUNDING_ANNUAL also remained absent in the last verified audit. ERROR_WEBHOOK_URL is optional monitoring, not a functional blocker.",
      "blocker": "Keep EMAILS_ENABLED and LIFECYCLE_EMAILS_ENABLED false until a real physical postal address and provider-issued delivery-webhook secret are configured. Keep checkout and Founding reservations paused until the account/mode-matched $99/year recurring Stripe Price is created and test-mode verification passes."
    },
    {
      "id": "GROWTH-PROD-DEPLOY",
      "area": "deployment",
      "description": "The reviewed growth build and its cron routes are deployed to the canonical production app",
      "status": "BLOCKED",
      "release_blocking": true,
      "evidence": "security/production-rollout-evidence.json preserves the initial production recovery authority. On 2026-07-11, revision 130d12a was built as unaliased production deployment dpl_8jhLseBMt8ZHQMWwDhfPbzAUTeec in the canonical showcase-app project; the rollback deployment at that checkpoint was dpl_3X2VgsCGKtGEEJ6fJWhiuvWsPuTN. Candidate probes returned 200 for root, one permanent redirect from /proofscore and nested legacy paths to root, 404 for retired score/reserve/stash APIs, no retired public copy in root or signup HTML, and green production health with commit 130d12a0e7a4. The exact candidate was then promoted without rebuild. Canonical https://app.tryshowcase.ink repeated the same probes and health result at 2026-07-12T01:19:24.256Z; https://tryshowcase.ink remained unchanged and reachable. The later public-copy cleanup is production deployment dpl_EQEH66PuFx8PfgAHsejTZgGTcUDB; Vercel inspection at 2026-07-12T02:46Z showed READY status in the canonical showcase-app project with app.tryshowcase.ink attached, and the canonical health endpoint remained HTTP 200 with database, Stripe, and OpenAI checks green. A subsequent exhaustive deployment audit found 187 historical READY showcase-app deployments whose public /proofscore HTML still contained the retired tool; all 187 were removed, including the obsolete git-main and test-integration aliases. The former dpl_3X2VgsCGKtGEEJ6fJWhiuvWsPuTN rollback was among those removed. Final READY inventory retains current clean dpl_EQEH66PuFx8PfgAHsejTZgGTcUDB, clean retirement rollback dpl_8jhLseBMt8ZHQMWwDhfPbzAUTeec, its clean preview, and three pre-feature deployments whose /proofscore routes return 404; canonical app health and redirect behavior remained green.",
      "blocker": "The backward-compatible build that removes the final claim route, legacy capacity helper, onboarding handoff, retention references, and proxy exemption is not yet staged or promoted. The required Production ABUSE_IP_HASH_SALT is now configured; after the hard grace boundary and post-boundary checks, stage the build unaliased, smoke-test it against the existing 047 schema, promote that exact build to canonical, and verify canonical routes plus authenticated Evidence Audit before applying migration 048. Once 048 drops the legacy objects, rollback is roll-forward only because an older build would query removed tables."
    },
    {
      "id": "GROWTH-PROVIDER-E2E",
      "area": "reliability",
      "description": "Production email, Stripe, referral, retention, and rollback paths pass controlled end-to-end smoke tests",
      "status": "BLOCKED",
      "release_blocking": true,
      "evidence": null,
      "blocker": "After migrations/config/deploy: test signed Resend events and suppression, one small refundable Stripe transaction, Founding hold expiry, referral completion credit, cron authentication, retention cleanup, and emergency kill switches."
    },
    {
      "id": "GROWTH-DISTRIBUTION-APPROVAL",
      "area": "distribution",
      "description": "Founder-approved X content and first partner wave are ready for manual release",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "The founder explicitly approved Partner Wave 1; the reviewed messages were submitted once through official routes and later corrected once to the canonical landing page, with four official-form receipts preserved in the campaign ledger. The current Buffer queue is empty, four clean native X posts remain scheduled, and all active machine-readable social and partner assets are portfolio-first with no /proofscore or waitlist destinations. The retirement regression and distribution validator both pass; historical exact-send records remain clearly marked and are not reusable instructions."
    },
    {
      "id": "P0-RACE",
      "area": "cost-abuse",
      "description": "AI quota, consumable credits, and pre-provider failures are atomic and user-fair",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "All authenticated OpenAI routes order abuse-attempt throttle -> dollar reservation -> atomic product quota/referral credit -> provider. Local proof passed 100 deterministic budget checks, 30/30 live dollar-ledger checks (including 10-way near-cap concurrency and RPC denial), and 10/10 Free-generation entitlement checks. Ten concurrent Free portfolio claims produced exactly one winner, nine clean denials, unchanged referral credits, one global-capacity increment, and a durable marker that survives portfolio deletion. The anonymous ProofScore provider/capacity path is retired: the final regression proves the score, reserve, stash, and claim routes plus the capacity helper are absent, while guarded migration 048 removes their three service-only RPCs and tables. Remaining public endpoints cannot spend the OpenAI budget."
    },
    {
      "id": "P1-SECRET-GUARD",
      "area": "secrets",
      "description": "Build-time server-only enforcement on every secret-holding module",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "None of src/lib/supabase/server.ts, src/lib/ai/client.ts, src/lib/ai/openai.ts, src/lib/ai/gemini.ts, src/lib/jobs/providers/index.ts, src/lib/stripe/client.ts had an explicit import 'server-only' guard. None were actually reachable from client code today (verified: build succeeds, which it would not if a client bundle imported a server-only-guarded module) but there was no enforced guarantee against a future accidental import. Added the guard to all six; typecheck/lint/build all still pass."
    },
    {
      "id": "P1-GLOBAL-CEILING",
      "area": "cost-abuse",
      "description": "Global AI request-count and dollar ceilings are atomic, explicit, locally verified, and production-configured before launch",
      "status": "BLOCKED",
      "release_blocking": true,
      "evidence": "`20260710033046_referral_abuse_and_credit_hardening.sql` contains the transactional request/bonus/global counter and an independent service-role-only general OpenAI dollar-reservation ledger. Local concurrency proof passed 30/30: exactly five of ten near-cap reservations won, denied IDs were not persisted, settlement/release/stale accounting was idempotent, and anon/authenticated RPC access was denied. Production now has migration 046, the $4/$80 general limits, a temporary request ceiling of 1, and KILL_SWITCH_AI=true; no billable production probe was run. Direct web-app Gemini paths remain fail-closed, and the separate $1/$20 Interview allocation must stay disabled until its atomicity gap is closed.",
      "blocker": "After rotating the exposed credentials, run a separately authorized controlled provider-backed proof of the $4/$80 production ledger and request ceiling. Keep KILL_SWITCH_AI=true, KILL_SWITCH_GEMINI=true, and INTERVIEW_KILL_SWITCH=true until that proof passes. Only enable Google paths after their separate $1/$20 allocation has an atomic ledger and concurrency proof."
    },
    {
      "id": "P1-STRIPE-PRICEID",
      "area": "stripe",
      "description": "Arbitrary client-supplied Price ID cannot redirect checkout to an unintended price",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "Live attack: POSTed {plan:'monthly', priceId:'price_EVIL_ARBITRARY_INJECTED', price_id:'price_EVIL_ARBITRARY_INJECTED'} to /api/stripe/create-checkout-session as a real authenticated user. Got 200 with a real Stripe checkout URL — the route ignores any client-supplied price field entirely and maps plan to a server-configured env-var price ID."
    },
    {
      "id": "P1-SSRF-RECHECK",
      "area": "ssrf",
      "description": "Independent re-verification of zero server-side user-URL fetches",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "Re-verified independently (not just trusting the prior P1-08 entry): every fetch() call site in src/lib (jobdataapi.ts, external.ts, error-reporter.ts, distributed.ts) uses a fixed, server-configured base URL; user input is only ever appended as a path segment or query param, never used as the origin/host. Zero fetch() calls exist directly in any API route."
    },
    {
      "id": "P1-DOCS",
      "area": "documentation",
      "description": "Cost controls, incident response, credential rotation docs exist",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "security/COST_CONTROLS.md, security/INCIDENT_RESPONSE.md, security/CREDENTIAL_ROTATION.md written this session, cross-referencing real verified state (kill switches, atomic rate limiter, BLOCKED items honestly marked where dashboard access wasn't available)."
    },
    {
      "id": "P2-NPM-AUDIT",
      "area": "supply-chain",
      "description": "npm audit reviewed, no critical/high vulnerabilities",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "3 moderate-severity findings, all the same PostCSS XSS advisory (GHSA-qx2v-qp2m-jg93) bundled inside Next.js's own internal node_modules/next/node_modules/postcss (v8.4.31) — confirmed every current Next release including 16.3.0 canary builds bundles the same version, so no non-breaking upstream fix exists yet. Not user-reachable: postcss here is a build-time CSS tool, never processes user-controlled CSS strings at runtime. The suggested 'npm audit fix --force' would downgrade Next 16.2.9 -> 9.3.3, a catastrophic breaking change explicitly avoided per this audit's instruction not to blindly force dependency upgrades. Accepted/monitored, zero critical or high severity findings."
    },
    {
      "id": "P0-01",
      "area": "authorization",
      "description": "Authorization matrix covers the current API route surface",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "security/api-authorization-matrix.json is a machine-enforced one-to-one inventory of all 82 API route files and all 102 exported HTTP handlers. The static audit passed 9/9, including 80 session handlers, exact method coverage, owner filters, cron secrets, provider signatures, and service-only RPC grants."
    },
    {
      "id": "P0-02",
      "area": "authorization",
      "description": "Adversarial authorization suite covers the current API surface",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "The canonical disposable-stack authorization audit passed 186/186 twice around the exhaustive account-deletion run. It covers all 80 anonymous session boundaries, cross-user records, public/token redaction, referral admission/capacity, cron secrets, signed Stripe/Resend webhooks, unsubscribe confirmation, and direct anon/authenticated attacks on service-only RPCs."
    },
    {
      "id": "P0-03",
      "area": "authorization",
      "description": "Stripe portal session ownership enforced",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "Verified by code read + test:authorization: customer_id derived server-side from session, never client input"
    },
    {
      "id": "P0-04",
      "area": "authorization",
      "description": "Cannot publish/export another user's resume or portfolio",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "test:authorization 15/15 covers publish, save, export, tailor, audit, generate-portfolio"
    },
    {
      "id": "P0-05",
      "area": "secrets",
      "description": "Credentials exposed in chat/terminal this session rotated",
      "status": "BLOCKED",
      "release_blocking": true,
      "evidence": "The account owner previously confirmed that earlier exposed credentials were rotated. During the 2026-07-10 local email-test diagnosis, the existing Resend API key value was printed into a private agent tool log. No provider call or email occurred and all email delivery remains disabled, but that newly exposed credential has not yet been rotated.",
      "blocker": "Revoke and replace the exposed Resend API key in Resend, local configuration, and the showcase-app production environment before any email feature or release is enabled."
    },
    {
      "id": "P0-06",
      "area": "secrets",
      "description": "gitleaks scan clean",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "Installed gitleaks via brew. Found 1 hit (hardcoded sk_test_placeholder fallback string, never a real key) and fixed it to no longer match secret patterns at all. .gitleaks.toml allowlists the now-historical commit. npm run test:secrets: no leaks found."
    },
    {
      "id": "P0-07",
      "area": "ai-truth",
      "description": "Truth Ledger fails closed under expanded adversarial set",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "test:truth-ledger 15/15 (existing scope)"
    },
    {
      "id": "P0-08",
      "area": "data-integrity",
      "description": "Account deletion covers the schema through canonical migration 047 and every storage bucket",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "The canonical local proof passed 20/20 after the 186-case authorization load: exact 48-migration ledger through 047; 29 CASCADE-owned tables; 8 intentional SET NULL relationships; all three discovered Storage buckets with flat, nested, and survivor files; forced Storage failure preserving the account; real authenticated route success; zero user/Auth residue; retained suppression/provider records; and complete synthetic cleanup."
    },
    {
      "id": "P0-RLS",
      "area": "authorization",
      "description": "Two-user adversarial RLS isolation covers canonical migrations through 047",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "A blank canonical local schema passed 16/16 core two-user/anonymous RLS attacks and 26/26 Interview Lab attacks. The suite proved cross-user SELECT/UPDATE/DELETE isolation, impersonation denial, server-authority field guards, published-only anonymous portfolio reads, zero client writes to interview accounting, and denial of reserve/commit RPC execution to anon and authenticated roles. Migration 047's exact grant matrix and deployment-time ACL assertions passed locally, and the completed production rollout's read-only audit found zero public tables without RLS, function ACL violations, or service-only table ACL violations."
    },
    {
      "id": "P0-UPLOAD",
      "area": "file-security",
      "description": "Magic-byte verification, size/MIME bucket enforcement",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "The eight-case upload suite passed magic-byte, MIME, size, and storage-boundary checks; the reviewed growth work did not change those validators."
    },
    {
      "id": "P0-XSS",
      "area": "xss",
      "description": "javascript:/data: URI rejection on public portfolio",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "The four-case public-portfolio XSS suite passed javascript: and data: URI rejection; the marketing truth pass did not weaken those sanitizers."
    },
    {
      "id": "P0-INJECTION",
      "area": "ai-security",
      "description": "Prompt injection resisted on live AI route",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "test:prompt-injection 9/9"
    },
    {
      "id": "P0-STRIPE-ENTITLEMENT",
      "area": "payments",
      "description": "Real checkout -> webhook -> DB entitlement activation works end to end",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "Verified live: real Stripe Checkout, real webhook signature, subscriptions.status=active confirmed via direct DB query"
    },
    {
      "id": "P1-01",
      "area": "stripe",
      "description": "Current Stripe webhook is idempotent and monotonic under retries and out-of-order delivery",
      "status": "BLOCKED",
      "release_blocking": true,
      "evidence": "The no-provider local suite passed 52/52 against canonical migrations: signature/tamper/staleness, exclusive claims, duplicates, busy/failed/stale retries, invoice states, paid/unpaid checkout validation, price/customer/user/plan conflicts, replacement ordering, atomic cancellation plus portfolio unpublish, lost-response retry, and Founding capacity/activation/release races. The configured real local Stripe credential is live-mode, so no Stripe API call was attempted.",
      "blocker": "Configure genuine Stripe test-mode credentials and complete a provider-backed checkout/webhook/reconcile proof. After controlled production promotion, run the separately authorized small refundable live transaction."
    },
    {
      "id": "P1-02",
      "area": "stripe",
      "description": "Invalid signature / wrong price / spoof rejection tests",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "test:stripe-webhook covers invalid signature; test:stripe-config covers price ID validity; test:authorization covers frontend Pro spoof"
    },
    {
      "id": "P1-03",
      "area": "stripe",
      "description": "Production webhook endpoint registered",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "User authorized a new, separate Vercel project (showcase-app, https://showcase-app-three.vercel.app — distinct from the waitlist-only casefile-ten.vercel.app, never touched). Registered a real Stripe webhook endpoint (we_1TkEgnRrWPEIfq2Mh8VT0W2r) against the deployed /api/stripe/webhook URL. Verified end-to-end with a real, completed monthly checkout (sub_1TkI8vRrWPEIfq2MMzBO89CO, customer cus_UjlfsiaMBM7PdI): Stripe delivered the webhook, signature verified, and the subscriptions row updated correctly (status=active, correct price_id, current_period_end, last_webhook_event_at populated) — confirmed by direct DB query, not just trusting the UI. HONEST CAVEAT: only the monthly path was exercised by a real completed checkout; the annual ($150) checkout session was generated and handed off but not completed by a human, so annual has config-level verification (test:stripe-config confirms the price exists/active) but not a completed live-webhook proof. The webhook handler is price-agnostic (does not branch on monthly vs annual), so risk is low, but this is not the same standard of evidence as monthly."
    },
    {
      "id": "P1-04",
      "area": "rate-limiting",
      "description": "Distributed rate limiter abstraction with local-dev fallback",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "AI-quota limiter was already Postgres-backed (distributed-safe), just non-atomic — low severity since quotas are generous and same-user races are rare/low-impact. Real gap was the waitlist IP limiter (genuine in-memory Map, single-instance only). Built src/lib/rate-limit/ (Upstash > Postgres-atomic > memory fallback) backed by rate_limit_increment() Postgres function (migration 011, single-statement atomic read+increment). Wired into waitlist/join. test:abuse 3/3 including a real concurrency test: 10 simultaneous requests, exactly 5 allowed — proves atomicity, not just sequential correctness."
    },
    {
      "id": "P1-05",
      "area": "rate-limiting",
      "description": "Upstash Redis wired for production",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "Read-only Vercel production environment inspection on 2026-07-09 confirmed both UPSTASH_REDIS_REST_URL and UPSTASH_REDIS_REST_TOKEN are configured as encrypted values; a newer production deployment exists after those variables were created. No secret value was read or printed."
    },
    {
      "id": "P1-06",
      "area": "auth",
      "description": "Session/cookie/redirect hardening verified via real browser tests",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "src/proxy.ts (Next.js 16 renamed Middleware->Proxy) already correctly refreshed Supabase SSR session cookies and gated protected routes — no rebuild needed there. Found and fixed a real open-redirect: src/app/(auth)/callback/route.ts read `next` from the query string and concatenated it unvalidated into a redirect target. Added safeNextPath() allowlisting same-app relative paths only. test:csrf 6/6 covers logout session invalidation + the open-redirect fix with 4 payload variants."
    },
    {
      "id": "P1-07",
      "area": "csrf",
      "description": "Origin/Host validation on cookie-authenticated state-changing routes",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "src/lib/security/origin-check.ts + wired into src/proxy.ts for all /api/* POST/PUT/PATCH/DELETE (Stripe webhook exempted — its own signature check is the real auth, and it's legitimately cross-origin). test:csrf 6/6: same-origin succeeds, forged cross-origin Origin is rejected 403, GET is never blocked, webhook exemption verified (fails on signature not Origin)."
    },
    {
      "id": "P1-08",
      "area": "ssrf",
      "description": "Zero server-side user-URL fetches, regression-tested",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "Code search confirmed zero fetch() calls on user input in any API route"
    },
    {
      "id": "P1-09",
      "area": "monitoring",
      "description": "Provider-neutral observability layer",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "src/instrumentation.ts using Next.js's native onRequestError hook (App Router, Next 16) — captures every uncaught server error across Server Components/Route Handlers/Server Actions in one place rather than per-route try/catch. src/lib/observability/error-reporter.ts redacts secret-shaped strings (Stripe keys, webhook secrets, Bearer tokens, JWTs) before logging. Verified redaction directly: a fake error containing a real-shaped sk_live_ key and a JWT was logged with both replaced by [REDACTED]."
    },
    {
      "id": "P1-10",
      "area": "monitoring",
      "description": "Error-monitoring adapter wired, app works without it",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "Zero-config path: structured JSON to console.error, captured by any host's log viewer. Optional ERROR_WEBHOOK_URL env var forwards the same redacted payload via a generic POST (works with a Slack webhook now, or a real Sentry/equivalent ingestion endpoint later) — no SDK dependency added, so the app's build/runtime is unaffected whether or not it's configured."
    },
    {
      "id": "P1-11",
      "area": "reliability",
      "description": "Emergency kill switches for AI/checkout/jobs/publishing",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "src/lib/feature-flags.ts, env-var gated (KILL_SWITCH_AI/CHECKOUT/JOBS_PROVIDER/PUBLISHING), default unset = enabled. Wired at the choke points: checkRateLimit() (covers all 9 AI-quota routes), create-checkout-session, jobs provider factory (forces fixture fallback), portfolio/publish (unpublish never blocked). test:kill-switches 4/4 — spawns real dev server instances with the env var set/unset and proves live behavior actually changes, not just that the code compiles."
    },
    {
      "id": "P1-12",
      "area": "reliability",
      "description": "Health check endpoints",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "src/app/api/health/route.ts — checks real DB connectivity + config presence (no secret values exposed), exempted from the waitlist lockdown so uptime monitors work regardless of launch state. test:kill-switches covers it (200 ok, no sk_ substring in body)."
    },
    {
      "id": "P1-13",
      "area": "backup",
      "description": "Production user data has a verified recoverable backup path appropriate to the launch stage",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "security/BACKUP_RESTORE.md documents the verified procedure; security/production-backup-evidence.json preserves restore proof for historical backup 20260710T152940Z, and security/production-rollout-evidence.json records fresh pre-migration backup 20260710T180210Z. The fresh authenticated AES-256-GCM archives contain one exported database snapshot and all 3 private Storage buckets (22 objects, 7,265,691 bytes). A network-disconnected disposable restore exactly reproduced 67 database objects, 2,008 table rows and content digests, the normalized public catalog, all application/Auth/Storage migration identities, the Auth signup trigger, nine Storage policies, and every Storage SHA-256. The target and volumes were destroyed and no plaintext remains. Supabase Free still lacks automated physical backups/PITR."
    },
    {
      "id": "P1-14",
      "area": "deployment",
      "description": "Safe local verification + deployment docs",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "DEPLOYMENT.md and LAUNCH_CHECKLIST.md define the canonical showcase-app target, web-only scope, fail-closed environment, restore-verified backup, completed paired database/application rollout, and recovered public/private route boundary. security/production-rollout-evidence.json preserves the exact migration, deployment, control, recovery, and negative-smoke proof. The legacy casefile-ten.vercel.app deployment remains an unused pre-consolidation backup and is not a beta target. The reusable runbook keeps credentialed/mutating suites outside immediate production smoke tests."
    },
    {
      "id": "P1-15",
      "area": "deployment",
      "description": "Canonical production deployment exists and is publicly reachable",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "Read-only verification after the 2026-07-10 public-route recovery: Vercel project showcase-app owns https://app.tryshowcase.ink; /api/health reports status ok for production commit 7b150783085b with database connectivity true. The root, pricing, career-services, and beta-feedback pages return 200, while unauthenticated signup and private product routes still redirect to /waitlist. Deployment identity and candidate/live smoke evidence are recorded in security/production-rollout-evidence.json."
    },
    {
      "id": "P1-16",
      "area": "email",
      "description": "Production authentication and lifecycle email are provider-verified and compliant",
      "status": "BLOCKED",
      "release_blocking": true,
      "evidence": "Local Standard Webhooks verification now passes signed/tampered Resend vectors and the invite cron reports explicit disabled/configuration gates. Production still lacks confirmed custom auth SMTP, a physical postal address, complete compliance variables, and provider E2E proof; the existing Resend API key was also exposed in a private diagnostic log and is treated as compromised.",
      "blocker": "Rotate the Resend API key, configure custom auth SMTP plus the missing sender/compliance variables and physical postal address, then test delivery, unsubscribe POST confirmation, bounce, complaint, and suppression before EMAILS_ENABLED=true."
    },
    {
      "id": "P1-17",
      "area": "ci",
      "description": "CI security workflow file exists",
      "status": "PASS",
      "release_blocking": false,
      "evidence": ".github/workflows/security.yml exists locally and was updated for the repository root, offline launch suite, and explicit live-test gating. A GitHub remote now exists, but the workflow change and required repository variables/secrets still need to be committed and configured before CI execution is proven."
    },
    {
      "id": "P1-18",
      "area": "headers",
      "description": "Security headers tightened + tested",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "test:headers 13/13. Removed script-src 'unsafe-eval' (verified via real production build + Playwright: signup flow, hydration, zero CSP console violations). Added frame-ancestors 'self' (modern clickjacking protection) and Strict-Transport-Security. Removed a stale/incorrect connect-src entry for api.anthropic.com (app uses OpenAI server-side only; browser never connects to either provider directly)."
    },
    {
      "id": "P1-19",
      "area": "privacy",
      "description": "Account deletion is verified against the current schema and retention policy",
      "status": "BLOCKED",
      "release_blocking": true,
      "evidence": "The 20/20 canonical local proof substantiates database/Auth/Storage deletion and the observed retained/unlinked rows, and public/internal privacy wording matches the fail-closed Storage behavior. A 2026-07-10 read-only production inventory classified the then-present 19 private objects and found 11 orphan candidates (781,697 bytes: 6 resumes and 5 interview recordings). The later full backup captured 22 objects but deliberately did not delete or reclassify anything.",
      "blocker": "Define and provider-test account deletion for an active Stripe subscription; with the verified backup preserved, obtain explicit approval and reconcile or remove the 11 private orphan candidates; then repeat the production-safe deletion/retention inspection after controlled migration promotion."
    },
    {
      "id": "P1-20",
      "area": "privacy",
      "description": "Privacy/retention documentation",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "security/PRIVACY_RETENTION.md was re-audited against canonical repository migrations through 047 and the completed dark production deployment. It covers Supabase/OpenAI/Stripe/Resend data flow, fail-closed recursive storage cleanup, 48-hour anonymous parse expiry, the once-daily retention schedule, eight-day hashed rate counters, 90-day terminal email ledgers, suppression retention, and the authenticated cleanup route. Provider-dependent guarantees remain explicitly gated while their features are disabled."
    },
    {
      "id": "P2-01",
      "area": "load",
      "description": "Load test scripts against local dev",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "scripts/load-test.mjs, no new dependency (plain concurrent fetch + percentiles). Run against a real production build at concurrency=50/300 req per endpoint: static pages (waitlist/pricing) ~30-65ms p50-p99, zero errors out of 900 total requests. /api/health is notably slower (p50 529ms, p95 775ms) since it does a real DB round-trip every call — fine at typical uptime-monitor polling intervals, but noted as a real finding, not glossed over."
    },
    {
      "id": "P2-02",
      "area": "e2e",
      "description": "Full 25-step paid-path E2E run",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "scripts/test-e2e-paid-path.mjs, 6/6 for what it covers: real signup, real billing page, real redirect to a genuine Stripe Checkout session with correct product/price, real test-card field entry, billing-portal route health. HONEST CAVEAT: does NOT complete actual payment submission — investigated directly (field values correct, button enabled, click registers) and concluded Stripe's own bot/fraud detection silently blocks a headless-automated submission, which is appropriate fraud protection, not something to try to defeat. The part this can't reach (webhook -> DB entitlement) is independently proven with real signed events in test-stripe-webhook.mjs, and was manually verified earlier this session (see P0-STRIPE-ENTITLEMENT). A human completing one real checkout manually would close this gap fully."
    },
    {
      "id": "P2-03",
      "area": "accessibility",
      "description": "Axe/Lighthouse pass",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "test:accessibility: 6/6 pages (waitlist/login/signup/pricing/privacy/terms) at ZERO violations of any severity (critical/serious/moderate/minor). Fully resolved this round: normalized all remaining text-muted-foreground opacity modifiers on /waitlist to full opacity (29 color-contrast nodes -> 0), added real <main> landmarks to all 6 public pages (fixed the landmark-one-main and region violations present on every page). No more honest caveats outstanding on this item."
    },
    {
      "id": "P2-04",
      "area": "visual-qa",
      "description": "Desktop+mobile screenshot review",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "scripts/capture-screenshots.mjs (gitignored output, regenerate on demand). Reviewed 12 real screenshots (6 pages x desktop/mobile). Found and fixed two real issues: (1) the dev-mode CSP had no carve-out for React's dev-only eval() usage, causing a visible Next.js dev-tools error badge on every page — fixed by making script-src conditionally include unsafe-eval only when NODE_ENV=development, re-verified production stays strict (test:headers still 13/13). (2) Three user-facing surfaces (privacy policy, homepage, in-app resume page) explicitly told users their resume is processed by 'Anthropic's Claude API' — factually wrong, the app uses OpenAI. This is a real privacy-policy accuracy issue, not cosmetic. Fixed all three plus stale references in README.md/SECURITY.md/AI_SETUP.md (the latter was the apparent source, fully rewritten to match the real OpenAI-based architecture)."
    },
    {
      "id": "IL-00",
      "area": "interview-lab",
      "description": "Interview Lab runtime and sub-feature switches fail closed and are configured explicitly",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "isInterviewLabRuntimeEnabled() requires both the paid-project attestation and Interview Lab switch; live, analysis, and recording also require their own flags. test:interview-live-config covers the fail-closed matrix. The account owner previously confirmed billing and terms review. The production variable names and a newer production deployment were verified read-only on 2026-07-09; encrypted values were not inspected, so individual switches remain an operator-controlled product decision."
    },
    {
      "id": "IL-01",
      "area": "interview-lab",
      "description": "Full question-bank coverage across all 10 advertised session types",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "Real bug found and fixed: a stale IMPLEMENTED_SESSION_TYPES allowlist in sessions/route.ts 400'd 7 of 10 session types even after question-bank templates existed for them. Removed the allowlist; buildInterviewPlan() itself fails closed for any genuinely unsupported type. test:interview-plan (loops all 10 types x 3 difficulties) + test:interview-session-types (real create->start->answer->list per type, 40/40)."
    },
    {
      "id": "IL-02",
      "area": "interview-lab",
      "description": "Story Bank: real UI on top of existing schema/API",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "test:interview-story-bank 11/11, real browser test."
    },
    {
      "id": "IL-03",
      "area": "interview-lab",
      "description": "Drills: deterministic (non-AI) scoring, real UI",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "15 drills, pure textual checks (word count, regex presence/absence) — explicitly not an AI judgment, UI does not claim otherwise. test:interview-drills 40/40 (deterministic) + test:interview-drills-ui 7/7 (real browser)."
    },
    {
      "id": "IL-04",
      "area": "interview-lab-security",
      "description": "Private report sharing: zero-anon-RLS-policy + hashed-token pattern, no transcript leak",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "interview_shared_reports has ZERO RLS policy for anon — the only resolution path is resolveSharedReport() via the service role, never a permissive policy. Token is randomBytes(32) base64url (256 bits); only its sha256 hash is persisted; the raw token is shown to the creator exactly once. A leaked DB row alone cannot be used to forge a valid share. Fixed an operator-precedence bug during development (access_count: x ?? 0 + 1 parsed as x ?? (0+1), so the counter never actually incremented) before it shipped. test:interview-sharing 18/18 adversarial."
    },
    {
      "id": "IL-05",
      "area": "interview-lab",
      "description": "Recorded Mode: now fully wired end-to-end with real Gemini audio transcription",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "transcribeAudio() (src/lib/interviews/gemini/transcription.ts) calls Gemini's multimodal generateContent with inlineData audio, gated by isInterviewAnalysisEnabled(). The recording route now synchronously transcribes on upload, creates a real voice_recorded transcript segment, and returns the same {answer, nextQuestion} shape the text-mode route does. Verified with genuinely real speech (macOS `say` + ffmpeg, not synthetic noise): scripts/test-interview-real-recording.mjs 8/8, transcription checked for actual semantic accuracy against the known source sentence, not just 'did it return a string.' Magic-byte audio validation, storage-layer file_size_limit/allowed_mime_types, owner-only path-prefix RLS unchanged from before. test:interview-audio-validation 18/18 + test:interview-recording 6/6 (updated to assert the real 201/transcript outcome now that the gate is on)."
    },
    {
      "id": "IL-06",
      "area": "interview-lab-privacy",
      "description": "Account deletion fully cascades Interview Lab data, including nested-path storage",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "Real gap found: the interview-recordings bucket nests one level deeper ($userId/$sessionId/$file) than the existing flat resumes-bucket cleanup pattern could traverse (Supabase Storage's list() is not recursive) — a deleted user's recording files would have been silently orphaned forever. Fixed with a nested-listing loop in /api/account/delete. scripts/test-interview-deletion.mjs (new) seeds a real session/story/drill/share plus a recording file via service role, deletes the account through the real route, and proves every interview_* table is empty AND the nested storage file is genuinely gone: 10/10."
    },
    {
      "id": "IL-07",
      "area": "interview-lab",
      "description": "Answer Retry: real attempts preserved, comparison is deterministic only",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "Mission's hard rule 'never claim improvement when deterministic metrics didn't improve' enforced directly: compareAttempts() reports only objective textual facts (word count delta, a number/outcome-language genuinely added, length change), never a fabricated quality score. Retry stores a new attempt_number, never overwrites the original. test:interview-retry-comparison 10/10 (deterministic, including a shorter/vaguer retry correctly reported as 'more concise' never 'improved') + test:interview-retry 8/8 (real browser + adversarial: cross-user 404, not-yet-completed-session 409)."
    },
    {
      "id": "IL-08",
      "area": "interview-lab",
      "description": "Results page: Save-as-Story is honest; Practice-Next personalization is now real, not just claimed",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "Save as Story copies the user's own real transcript answer verbatim into Story Bank — zero invention. REAL BUG FOUND AND FIXED: Practice Next's copy already said 'targeted at your weakest scored dimensions' whenever an evaluation existed, but the actual drill-recommendation call always passed an empty array (recommendDrillsForDimensions([])) — the personalization claim was never backed by real data, even before this session's Gemini work. Fixed by computing real weak dimension_ids from interview_dimension_scores (score < 70, ascending) and passing them through; added an honest middle case ('scored well across every dimension') for sessions with no weak dimensions, distinct from the original disabled-gate fallback. test:interview-results-extras 5/5, including a check that the page now reflects real per-dimension scores rather than the old fallback text."
    },
    {
      "id": "IL-09",
      "area": "accessibility",
      "description": "WCAG AA color-contrast: Interview Lab pages + a pre-existing app-wide Sidebar bug",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "scripts/test-accessibility.mjs only ever scanned public marketing pages, so a contrast bug in the always-rendered Sidebar (text-muted-foreground/60 on the user's email and the billing upsell link, measured 2.85:1 and 3.51:1 by axe against the 4.5:1 AA threshold) had never been caught — it affects every authenticated page in the whole app, not just Interview Lab. Found via scripts/test-interview-accessibility.mjs (new), fixed in the Sidebar plus every text-muted-foreground/NN instance introduced in Interview Lab this session. test:interview-accessibility 10/10 (7 real authenticated pages + 3 manual keyboard/camera checks); test:accessibility (public pages) re-confirmed 10/10, no regression."
    },
    {
      "id": "IL-10",
      "area": "compliance",
      "description": "Age-eligibility gate removed at the user's explicit, confirmed instruction",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "The gate (DB columns, API checks, Lobby checkbox UI) existed to satisfy a stated Gemini ToS compliance requirement. Removed via migration 019 only after explicit user confirmation through AskUserQuestion (a destructive-migration auto-mode classifier had blocked the first attempt, citing the prior compliance requirement). This is a deliberate accepted-risk product decision by the account owner, not a verified compliance finding — documented here so the tradeoff is visible, not silently dropped."
    },
    {
      "id": "IL-11",
      "area": "interview-lab",
      "description": "Real Gemini analysis wired end-to-end; critical schema-rejection bug found and fixed",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "CRITICAL FINDING, FIXED AND VERIFIED. zod's z.toJSONSchema() output is rejected outright by Gemini's responseJsonSchema (400 INVALID_ARGUMENT, no detail) — found via methodical binary-search isolation testing directly against the live API. Unsupported keywords: top-level $schema, additionalProperties (anywhere), minItems/maxItems/minLength/maxLength (anywhere), anyOf-with-a-null-branch. This bug existed in code from prior sessions that had never been run against a real key, and an IDENTICAL latent bug existed separately in the pre-existing (unrelated) AI-reviewer feature in src/lib/ai/gemini.ts — also fixed as a result. Fix: src/lib/ai/gemini-schema-utils.ts (toGeminiJsonSchema()), wired into both callers. scripts/test-gemini-schema-utils.mjs 11/11 deterministic (caught and fixed a bug in the sanitizer itself: the anyOf-null-merge logic reintroduced stripped keywords via Object.assign before sanitizing the merged branch). scripts/test-interview-real-analysis.mjs 8/8 against the real live API: real numeric score, real model name, real dimension explanations, and every cited evidence segment ID verified to map to a REAL transcript segment (never fabricated). test:interview-e2e's Results-page check now asserts a real numeric score from real analysis rather than the old disabled-gate message."
    },
    {
      "id": "IL-12",
      "area": "interview-lab",
      "description": "Live voice mode (Gemini Live API) actually wired and tested end to end with real audio",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "Full real architecture verified against the live, billed Gemini project: server mints a short-lived ephemeral token via authTokens.create() (requires httpOptions.apiVersion: 'v1alpha' — discovered by testing, undocumented in the default API version) with the interviewer persona + exact question list locked into liveConnectConstraints; the browser connects DIRECTLY to Gemini's Live WebSocket using ONLY that token (confirmed: the real GEMINI_API_KEY never reaches the browser, verified by a standalone client instance authenticated with just the token). src/lib/interviews/live-voice-client.ts captures mic audio (ScriptProcessorNode, downsampled to 16kHz PCM16), streams it via sendRealtimeInput, and plays back the model's real spoken responses (24kHz PCM scheduled via Web Audio API). TWO REAL BUGS FOUND: (1) Transcription.finished, documented as the segment boundary, never actually arrives true for this model — the real boundary is serverContent.turnComplete for the interviewer's side and 'a new output chunk starting' for the candidate's side; found via raw message logging against the live API, not assumption. (2) the CSP's connect-src and the Permissions-Policy's microphone=() unconditionally blocked the browser->Gemini WebSocket and all mic access respectively — both fixed in next.config.ts. Verified end-to-end with scripts/test-interview-live-voice-real.mjs: Chromium launched with --use-file-for-fake-audio-capture fed a genuine spoken-word WAV (macOS `say` + ffmpeg, not synthetic noise) as the simulated microphone; 9/9 passing, including the model asking real questions, the candidate's actual spoken words transcribed near-verbatim, and both sides persisted as real voice_live transcript segments via the new live-transcript route."
    },
    {
      "id": "IL-13",
      "area": "interview-lab",
      "description": "Two real pre-existing/incidental bugs found while wiring voice mode end-to-end",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "(1) Session creation (sessions/route.ts) unconditionally 403'd deliveryMode:'voice' regardless of isInterviewLiveEnabled()'s actual value — a hardcoded rejection that would have kept blocking voice sessions even after the flag was correctly flipped on. Fixed to check the real flag. (2) src/hooks/use-user.ts used .single() for the subscriptions query, which PostgREST 406s on zero rows — every free user (the majority) hitting any page that calls useUser() got a console 406. This was previously unused anywhere in the app (confirmed: zero callers before this session); first exercised when /interviews/new was built to show a Pro badge, surfacing a pre-existing latent bug as a real console error in test:interview-e2e. Fixed both .single() calls to .maybeSingle()."
    },
    {
      "id": "IL-14",
      "area": "interview-lab",
      "description": "New Interview UI restructured: Live Interview vs Written as 2 top-level types, 10 subcategories nested under each",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "/interviews/new is now a 2-step wizard: step 1 picks delivery mode (Live Interview, with a Pro badge for non-Pro users; Written), step 2 shows the same 10 session-type subcategories plus difficulty/length, labeled 'Question style' under whichever delivery mode was chosen. Lobby page now reflects the real delivery_mode (mic-required copy for voice, no-mic copy for text) instead of a hardcoded 'Text' string. Found and fixed a real WCAG AA contrast violation introduced by the new Pro badge (text-brand-500 at 11px on dark background, 3.89:1 vs the required 4.5:1) — fixed by switching to a pill badge with text-brand-300. test:interview-accessibility 10/10, test:interview-e2e 16/16, test:interview-authorization 16/16 (updated to test the real post-gate behavior: voice session creation now succeeds, live-token still correctly enforces Pro + ownership) — all re-run and passing after the restructure."
    },
    {
      "id": "IL-NOT-STARTED-EVAL",
      "area": "interview-lab",
      "description": "Prompt-evaluation harness, scoring constitution document, golden dataset",
      "status": "NOT_STARTED",
      "release_blocking": false,
      "evidence": null,
      "blocker": "Not yet built. Now meaningful since Gemini analysis is genuinely enabled and producing real scored evaluations, but still not built this session."
    },
    {
      "id": "IL-15",
      "area": "interview-lab",
      "description": "Interview Hub redesigned into a readiness-led command center: deterministic role+type-aware readiness model, server-controlled recommendation engine, evidence coverage, job-specific mode",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "The prior Hub (src/app/(app)/interviews/page.tsx) averaged overall_score across ALL evaluations regardless of session type or target role, and one stat card read 'Voice interviews: Coming soon' — stale now that Live voice is real. Replaced with: (1) src/lib/interviews/readiness.ts — computeReadinessGroups() groups sessions by targetRole+sessionType, excludes stale rubric_version and mismatched coaching_mode rather than averaging across them, surfaces sample size/trend/strongest+priority dimension; never shows a number without a sample-size and rubric disclosure. (2) src/lib/interviews/recommendations.ts — computeNextActions(), a pure, deterministic, fully server-controlled function; Gemini supplies weakness data but never picks the action (REAL BUG FOUND AND FIXED during build: the drill recommendation's reason text always cited the single lowest-scoring dimension even when the catalog's matching drill targeted a different, but still genuinely weak, dimension — fixed to cite whichever dimension the chosen drill actually addresses). (3) src/lib/interviews/evidence-coverage.ts — maps the 10 canonical Story Bank competencies (now centralized in competencies.ts, previously duplicated in story-bank/page.tsx) to real coverage. (4) src/lib/interviews/hub-data.ts (server-only) — single parallel-fetch loader, no client waterfall. New components under src/components/interviews/hub/ and shared/: command header, readiness overview with a 'how this was calculated' disclosure drawer, a non-radar Strong/Developing/Focus-Here dimension view with a real <table> fallback, next-actions, evidence coverage, recent sessions, practice resources, privacy summary, job-specific banner (wired through real saved_jobs data, /interviews/new now accepts and prefills savedJobId/targetRole/targetCompany query params, previously dead-ended), and a dedicated new-user empty state. 'Interview Readiness is a structured practice diagnostic — not a hiring prediction' is shown wherever a readiness number appears. Tested: test:interview-readiness 27/27 and test:interview-recommendations 17/17 (deterministic, pre-UI), test:interview-hub 21/21 (new, real-browser, full loop: new user -> baseline session -> real Gemini analysis -> readiness appears -> retry -> save story -> evidence coverage updates -> save a real job -> job-specific mode -> privacy section -> hard-refresh persistence -> mobile no-overflow -> cross-user isolation, both via UI text and direct API 404), test:interview-accessibility 11/11 (added a second /interviews scan covering the populated returning-user state, not just the empty state), full existing interview regression suite re-run after the change (see IL-16). Found and fixed one real mobile-only visual bug via screenshot inspection: the command header's primary CTA used a long dynamic action title inside a whitespace-nowrap pill button, causing the text to visibly overflow the button's rounded edges at 390px width — fixed to wrap and grow the button height instead of clipping. KNOWN, DELIBERATE SCOPE LIMIT: the originating brief's 'Assignments and Invitations' Hub section is not built — no invitations/assignments schema exists anywhere in this codebase, and fabricating one was judged out of scope for this pass; the Hub omits that section entirely rather than showing a fake or empty placeholder for it."
    },
    {
      "id": "IL-16",
      "area": "interview-lab",
      "description": "Full existing Interview Lab regression suite re-run after the Hub rewrite, two real stale-assertion bugs found and fixed",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "Re-ran every existing interview test script after the Hub rewrite. test:interview-e2e: 1 failure — its Hub assertion looked for an exact-text link 'start an interview' that no longer exists now that the new-user state's real CTA reads 'Start your baseline interview'; fixed the assertion to match the new, intentional copy (now 16/16). test:interview-recommendations: after fixing the IL-15 drill-citation bug, the existing test fixture didn't carry a `dimensions` array the fixed code now reads; updated the fixture (still 17/17, now exercising the fix). test:interview-drills-ui had one flaky failure ('too-short answer shows a failed check') that passed cleanly on an immediate re-run with no code changes — confirmed pre-existing flakiness unrelated to this change, not a regression. All other suites passed clean on first run with zero changes needed: test:interview-rls 24/24, test:interview-authorization 16/16, test:interview-session-types 40/40, test:interview-story-bank 11/11, test:interview-sharing 18/18, test:interview-recording 6/6, test:interview-deletion 10/10, test:interview-retry 8/8, test:interview-results-extras 5/5, test:interview-question-safety 91/91, test:interview-rubrics 147/147, test:interview-scoring 9/9, test:interview-plan 163/163, test:interview-live-config 30/30, test:interview-drills 40/40, test:interview-audio-validation 18/18, test:interview-retry-comparison 10/10, test:interview-real-analysis 8/8 (live Gemini), test:interview-real-recording 8/8 (live Gemini), test:interview-live-voice-real 9/9 (live Gemini Live, real audio fixture). npm run typecheck, lint, and build all clean after every fix."
    },
    {
      "id": "IL-NOT-STARTED-VISUALQA",
      "area": "visual-qa",
      "description": "Visual QA screenshot review across Interview Lab's ~25 UI states",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "RESOLVED 2026-07-02: full-app visual QA sweep executed (not just Interview Lab): every public page + fresh-signup -> resume parse -> portfolio generation -> builder flow + all authenticated surfaces captured at desktop (1440px) and mobile (390px), scroll-position captures for animation-gated sections, screenshots individually inspected. Found and fixed: builder live-preview fixed-position paint escape (P1), free-tier first-value paywall (P0 product defect), jobs default-relevance + duplicate chips, ProofScore prefill, stale beta CTAs, unlimited-interviews claim, WCAG contrast on landing/pricing/interview-hub. Mobile: zero horizontal overflow on all checked pages.",
      "blocker": null
    },
    {
      "id": "IL-NOT-STARTED-DEPLOY",
      "area": "deployment",
      "description": "Gemini Interview Lab env vars deployed to Vercel production",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "Read-only `vercel env ls production` on 2026-07-09 confirmed GEMINI_API_KEY, GEMINI_PAID_PROJECT_CONFIRMED, GEMINI_INTERVIEW_ENABLED, INTERVIEW_LIVE_ENABLED, INTERVIEW_ANALYSIS_ENABLED, and INTERVIEW_RECORDING_ENABLED as encrypted production values. The latest production deployment was created after those variables. This verifies deployment presence, not the product decision to keep any individual flag true or false."
    },
    {
      "id": "IL-17",
      "area": "interview-lab",
      "description": "Server-side entitlement system: atomic quota enforcement for Free (2 sessions) / Pro (15 sessions, 8 audio) / retries, with real adversarial concurrency proof",
      "status": "PASS",
      "release_blocking": true,
      "evidence": "Built the complete canonical server-side entitlement service (src/lib/interviews/entitlements/). The browser displays usage but never decides plan/quota/audio-allowance/retry-allowance. DB migrations 022-025: interview_usage_reservations table + atomic reservation/commit/release RPCs using pg_advisory_xact_lock per (user_id, kind) to serialize concurrent callers — same atomic pattern that previously fixed the AI-quota race (P0-RACE). Three reservation kinds: session (period quota), audio_session (Pro-only sub-pool, every audio reservation also takes a session slot), retry (Pro period pool / Free per-session-one). Reservation lifecycle: reserved → committed (first real answer) → released (pre-answer abandon or narrow analysis-failure refund). All 6 session-modification routes wired: session creation reserves before inserting (rollback on fail), transcript/live-transcript/recording routes commit on first answer, DELETE refunds only pre-answer reservations, retry route reserves from the correct pool and releases-specific on DB race-lost. GET /api/interviews/usage surfaces real used/remaining/reset-date per signed-in user. BUGS FOUND AND FIXED: (1) PostgREST 'function is not unique' ambiguity between 6-param (migration 022) and 7-param (migrations 023/024) overloads — PostgREST raises an error when omitting the new parameter, which was misinterpreted as AUDIO_LIMIT_REACHED for a brand-new Pro user on their first session. Fixed by always passing p_max_concurrent: null explicitly (7-param call unambiguous). Migration 025 saves the DROP of the 6-param orphan locally — see KNOWN GAP below. (2) Misleading error message: audioErr truthy (RPC routing failure) and !audioRes.allowed (genuine quota denial) were conflated — now distinguished with separate, honest messages. (3) Retry race: the non-atomic priorRetryCount SELECT let parallel same-answer retries both pass the quota gate; the DB unique-constraint on (question_id, attempt_number) is the real final arbiter, now correctly surfaced as 409 RETRY_RACE_LOST instead of 500, with releaseSpecificReservation cleaning up the losing reservation without touching any other reservation on the session. test:interview-entitlements 24/24 (deterministic: plan constants, session-type gating, env ceilings, Free calendar-month boundaries, Pro Stripe billing-period math). test:interview-limits 13/13 (real adversarial concurrency: ≤2 Free sessions under 10 parallel creates, correct denial codes; 8th audio slot race with 3-burst after 7 sequential completions; 0 orphaned reservations after parallel burst; 1 Free retry under 5 simultaneous requests, 4 RETRY_RACE_LOST not 500s; slot refund on abandoned session; no refund on answered session). resolvePlanContext() delegates entirely to the canonical isProUser() helper — confirmed same code path used by the rest of Showcase. KNOWN DOCUMENTED GAPS: (a) Migration 025 (DROP 6-param overload) saved locally but not yet applied to the live DB — code workaround active (explicit p_max_concurrent: null) provides complete protection, but the migration should be applied before the code workaround is ever removed. (b) Free retry enforcement is non-atomic across DIFFERENT questions in the same session: the non-atomic priorRetryCount SELECT could let a user fire retries on question-1 and question-2 simultaneously and both pass (each sees count=0 before the other's DB write lands); the DB unique constraint only catches same-question parallel retries. Since Free is limited to 1 retry per session total, this is a real but narrow bypass vector (requires >1 question per session AND simultaneous cross-question retries). Full fix requires an atomic per-session retry RPC, deferred."
    },
    {
      "id": "IL-NOT-STARTED-COST-MODEL",
      "area": "cost-control",
      "description": "Interview Lab cost-model document (growth/interviews/cost-model.md)",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "RESOLVED 2026-07-02: growth/interviews/cost-model.md written from the code's real RATES (budget.ts) and plan limits (plans.ts): per-action costs (text session ~$0.02-0.04 all-in, live voice ~$0.017/min, retries/drills $0), Free/Pro monthly exposure incl. worst-case and pathological rows, margin math (realistic Pro ~91%, ceiling ~27%, pathological negative -> why budget gates exist), and the four server-side backstops (atomic reservations, spend budgets w/ recommended production values, 30-min hard ceiling, kill switch). interview_cost_events ledger (migration 028, now applied live) records real spend for reconciliation.",
      "blocker": null
    },
    {
      "id": "IL-NOT-STARTED-ATOMIC-FREE-RETRY",
      "area": "interview-lab",
      "description": "Atomic Free-tier retry enforcement across different questions in the same session",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "RESOLVED 2026-07-01: migration 027 applied to the live DB (see MIG025-APPLY row). test:interview-limits 16/16 — 5 simultaneous retries on one Free session: exactly 1 succeeds; cross-question parallel retries: exactly 1 succeeds, no orphaned reservations; deletion refund semantics verified.",
      "blocker": null
    },
    {
      "id": "IL-NOT-STARTED-MIG025-APPLY",
      "area": "database",
      "description": "Apply migration 025 (DROP 6-param overload) + 027 (atomic retry RPC) to live Supabase DB",
      "status": "PASS",
      "release_blocking": false,
      "evidence": "RESOLVED 2026-07-01: migrations 025 (drop 6-param overload), 026 (portfolio-images bucket + RLS policies), 027 (interview_reserve_retry atomic RPC), and 028 (interview_cost_events ledger) all applied to live project yogwhfrjhcbnvoxitcay via Supabase MCP apply_migration; migration ledger now records them. Verified live: npm run test:interview-limits 16/16 including the parallel-retry races that exercise the 027 RPC directly.",
      "blocker": null
    }
  ]
}
