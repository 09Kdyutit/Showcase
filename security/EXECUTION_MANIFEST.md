# Showcase — Release Execution Manifest

**STATUS as of 2026-06-19 (end of this session): every preventable item is PASS.** Only 3 release-blocking items remain, and all 3 are genuinely BLOCKED on external human action (P0-05 credential rotation, P1-03/P1-15 deployment — the existing Vercel project is explicitly off-limits, see notes below). `npm run release:gate` exits 1 solely because of these 3 — there is nothing further to build, fix, or test without that human action.

**NEXT ACTION (resume point) for a future session:** Check `npm run release:gate` first — if new code has been added since this was written, re-verify the rows below it touches before assuming they still hold. If still NOT READY only on P0-05/P1-03/P1-15, the only path forward is the human actions listed in their `Blocker` column; there is no further autonomous work available until one of them is resolved. If P0-05 has been resolved (credentials rotated), flip its status and re-run `npm run release:gate`. If a real launch deployment target has been authorized (NOT the existing waitlist-only Vercel project), P1-03/P1-15 become workable: deploy, register the Stripe webhook against the real URL, re-run `npm run test:stripe`, flip statuses.

**Baseline confirmed this session (2026-06-19):** clean git tree at `2e9ba51` at session start, all 11 migrations applied (001-011), full regression suite passing throughout (test:match 17, test:taxonomy 17, test:truth-ledger 15, test:ats 7, test:rls 13, test:uploads 8, test:xss 4, test:prompt-injection 9, test:authorization 15, test:deletion 13, test:csrf 6, test:abuse 3, test:stripe 14, test:kill-switches 4, test:accessibility 5/6 pages clean, test:headers 13, verify.mjs 60/60), typecheck/lint/build clean throughout. 107+ test accounts created during testing were cleaned up via `scripts/cleanup-test-accounts.mjs` (user-approved).

Status values: `NOT_STARTED` / `IN_PROGRESS` / `PASS` / `FAIL` / `BLOCKED`.

## P0 — Critical (auth bypass, cross-user access, payment integrity, secrets, RCE, data loss)

| ID | Release area | Exact task | Status | Implementation files | Verification command | Evidence | Blocker | Next action | Commit |
|---|---|---|---|---|---|---|---|---|---|
| P0-01 | Authorization | API ownership matrix across all 21 routes | PASS | `security/API_AUTHORIZATION_MATRIX.md` | manual + `test:authorization` | Zero missing ownership checks across all 21 routes | — | done | 90cb7fc |
| P0-02 | Authorization | Adversarial cross-user test: saved_jobs, applications, tailored_assets, portfolios, resumes, Stripe portal via real API calls | PASS | `scripts/test-api-authorization.mjs` | `npm run test:authorization` | 15/15. Found+fixed 2 real bugs: PATCH on jobs/save and applications returned 500 (PGRST116, `.single()` on zero-row UPDATE) instead of 404 when targeting another user's resource | — | done | 90cb7fc |
| P0-03 | Authorization | Stripe portal session cannot be opened for another user's customer | PASS | `src/app/api/stripe/create-portal-session/route.ts` | `npm run test:authorization` | Confirmed: customer_id always derived from session, never accepts client input | — | done | 90cb7fc |
| P0-04 | Authorization | Publishing/exporting another user's resume or portfolio is rejected | PASS | `scripts/test-api-authorization.mjs` | `npm run test:authorization` | Covered in the 15/15 above | — | done | 90cb7fc |
| P0-05 | Secrets | Rotate credentials pasted in chat this session (Stripe keys, Supabase service role, DB password) | BLOCKED | — | manual | Flagged twice already | Requires user to rotate in Stripe/Supabase dashboards — only they can authorize/execute this | Document exact steps; await user action | — |
| P0-06 | Secrets | gitleaks scan of full repo + git history | PASS | `.gitleaks.toml`, `src/lib/stripe/client.ts` | `npm run test:secrets` | Found 1 hit (placeholder string shaped like a Stripe key, never real); fixed the string + added allowlist for the historical commit. Clean scan now. | — | done | pending |
| P0-07 | AI Truth | Truth Ledger export-block still fails closed under expanded adversarial set | PASS | `scripts/test-truth-ledger.mjs` | `npm run test:truth-ledger` | 15/15 (existing scope from prior session, re-verified clean this session) | — | done | 2e9ba51 |
| P0-08 | Data loss | Account deletion cascades correctly across all 14 tables + storage | PASS | `src/app/api/account/delete/route.ts`, `src/app/(app)/settings/page.tsx`, `scripts/test-account-deletion.mjs` | `npm run test:deletion` | 13/13. Was a non-functional stub before this session (button just toasted "contact support"). Built real deletion: type-DELETE-to-confirm dialog -> auth.admin.deleteUser cascades all 14+ tables (all have ON DELETE CASCADE) + explicit storage cleanup. Verified deleted user cannot log back in. | — | done | pending |

## P1 — High (production webhook, rate limiting, session hardening, CSRF, monitoring, rollback, backups)

| ID | Release area | Exact task | Status | Implementation files | Verification command | Evidence | Blocker | Next action | Commit |
|---|---|---|---|---|---|---|---|---|---|
| P1-01 | Stripe | Webhook event idempotency (duplicate/replayed events don't double-apply) | PASS | `src/app/api/stripe/webhook/route.ts`, migration 010 | `npm run test:stripe-webhook` | 6/6. Added `processed_webhook_events` table (unique event_id) + `last_webhook_event_at` out-of-order guard. Real test: signed duplicate event short-circuits, older out-of-order event doesn't regress newer status. | — | done | pending |
| P1-02 | Stripe | Invalid signature, wrong price ID, frontend Pro spoof all rejected | PASS | `scripts/test-stripe-config.mjs`, `scripts/test-stripe-webhook.mjs` | `npm run test:stripe` | 14/14 combined (8 config + 6 webhook) | — | done | pending |
| P1-03 | Stripe | Production webhook endpoint registered | PASS | Stripe webhook endpoint `we_1TkEgnRrWPEIfq2Mh8VT0W2r` | Real completed monthly checkout | Registered against `https://showcase-app-three.vercel.app/api/stripe/webhook`. Verified via a real completed monthly checkout (`sub_1TkI8vRrWPEIfq2MMzBO89CO`) — webhook delivered, signature verified, DB row updated correctly (confirmed by direct query, not just UI). Annual path has config-level verification only (price active in Stripe) — no completed annual checkout exists yet; same code path handles both so risk is low, but evidence is asymmetric. | — | done | pending |
| P1-04 | Rate limiting | Distributed rate limiter abstraction (memory fallback for local dev) | PASS | `src/lib/rate-limit/*`, migration 011, `src/app/api/waitlist/join/route.ts` | `npm run test:abuse` | 3/3 incl. real concurrency test (10 simultaneous requests, exactly 5 allowed). AI-quota limiter was already Postgres-backed; real gap was waitlist's in-memory Map, now replaced. | — | done | pending |
| P1-05 | Rate limiting | Upstash Redis wired for production | BLOCKED | — | manual | — | Requires Upstash account creation + API token | User must create account and provide `UPSTASH_REDIS_REST_URL`/`TOKEN` | — |
| P1-06 | Auth | Session/redirect/cookie hardening real-browser tests | PASS | `src/app/(auth)/callback/route.ts`, `scripts/test-csrf.mjs` | `npm run test:csrf` | 6/6. `src/proxy.ts` (Next 16 renamed Middleware->Proxy) already did SSR session refresh + route gating correctly. Found+fixed a real open-redirect: `next` query param in the auth callback was concatenated unvalidated into a redirect; added `safeNextPath()` allowlisting relative paths only. | — | done | pending |
| P1-07 | CSRF | Origin/Host validation helper for cookie-authenticated state-changing routes | PASS | `src/lib/security/origin-check.ts`, `src/proxy.ts` | `npm run test:csrf` | 6/6 (combined with P1-06 in the same script). Forged cross-origin Origin on a state-changing /api/* request -> 403. Stripe webhook exempted correctly (its signature check is the real auth). | — | done | pending |
| P1-08 | SSRF | Confirm zero server-side user-URL fetches (re-verify after this session's new routes) | PASS (prior session) | — | `npm run test:ssrf` | Verified via code search: zero `fetch()` calls in any API route on user input | — | Add automated test script for regression protection | 2e9ba51 |
| P1-09 | Monitoring | Provider-neutral observability layer (request IDs, error adapter, redaction) | PASS | `src/instrumentation.ts`, `src/lib/observability/error-reporter.ts` | manual (verified redaction logic directly) | Uses Next.js 16's native `onRequestError` hook — catches every uncaught server error in one place. Redacts Stripe keys/webhook secrets/Bearer tokens/JWTs before logging; verified directly with a fake error containing a real-shaped secret. | — | done | pending |
| P1-10 | Monitoring | Sentry (or equivalent) wired via env var, app works without it | PASS | `src/lib/observability/error-reporter.ts` | manual | Zero-config: structured console logging always works. Optional `ERROR_WEBHOOK_URL` forwards the same redacted payload via generic POST — no SDK dependency added either way. | — | done | pending |
| P1-11 | Reliability | Kill switches: AI routes, checkout, jobs provider, public publishing | PASS | `src/lib/feature-flags.ts`, wired in `checkRateLimit`, `create-checkout-session`, jobs provider factory, `portfolio/publish` | `npm run test:kill-switches` | 4/4. Spawns real dev server instances with/without the env var set and confirms live HTTP behavior actually changes (503 with switch on, normal otherwise). Unpublish is deliberately never blocked. | — | done | pending |
| P1-12 | Reliability | Health check (liveness/readiness) endpoints, no secret leakage | PASS | `src/app/api/health/route.ts`, `src/proxy.ts` (exempted from waitlist lockdown) | `curl /api/health`, covered in `test:kill-switches` | Real DB connectivity check + config presence, no secret values in response | — | done | pending |
| P1-13 | Backup | Backup/restore documentation + Supabase PITR confirmation | PASS | `security/BACKUP_RESTORE.md` | manual | Checked the live Supabase org directly: plan is `free` — zero automated backups, zero PITR. Real, currently-unmitigated risk, now documented with upgrade path + interim manual pg_dump mitigation. Documentation is done; actual backup coverage is a separate billing decision flagged in LAUNCH_CHECKLIST.md. | Real restore drill still needs a disposable project | Document done; drill is a launch-blocker, not a doc-blocker | pending |
| P1-14 | Deployment | Staging env config + DEPLOYMENT.md/LAUNCH_CHECKLIST.md | PASS | `DEPLOYMENT.md`, `LAUNCH_CHECKLIST.md` | manual | Fixed stale `AI_API_KEY`->`OPENAI_API_KEY`, added missing env vars (`LAUNCH_OPEN`, annual price, Upstash), added pre-deploy verification step, reconciled checklist against what's actually done. | — | done | pending |
| P1-15 | Deployment | Actual staging deployment | PASS | `vercel.json`, Vercel project `showcase-app` | `curl` smoke tests, `/api/health` | Live at `https://showcase-app-three.vercel.app` (separate project from waitlist-only `casefile-ten.vercel.app`, user-confirmed before creation). Found and fixed a real platform bug along the way: a freshly `vercel project add`-created project defaults Framework Preset to "Other", which silently breaks all dynamic routing (every route 404'd) — fixed via explicit `vercel.json` with `framework:"nextjs"`. Disabled SSO deployment-protection (user-confirmed) since Stripe webhooks and human checkout testing both need unauthenticated public access. | — | done | pending |
| P1-16 | Email | Production SMTP / confirmation / reset readiness docs | PASS | `security/EMAIL_READINESS.md`, `src/app/(auth)/signup/page.tsx`, `src/app/(auth)/login/page.tsx` | manual | Found+fixed a real bug while documenting: emailRedirectTo pointed at `/auth/callback`, which doesn't exist — the `(auth)` route group is stripped from the URL, real path is `/callback`. Email confirmation + magic-link sign-in were broken. Also documented: no custom SMTP, relying on Supabase's dev-only default email service. | Actual SMTP provider account is user's decision | done | pending |
| P1-17 | CI | GitHub Actions security workflow | PASS | `.github/workflows/security.yml` | YAML-validated via js-yaml; cannot actually execute (no remote) | Static job (typecheck/lint/build/verify/gitleaks) needs zero secrets. Live job (full adversarial suite + release:gate) needs repo secrets: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY, SUPABASE_SERVICE_ROLE_KEY, STRIPE_SECRET_KEY, NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRICE_ID_PRO_MONTHLY/ANNUAL, OPENAI_API_KEY — skips cleanly (not fails) until set. | No git remote configured | File is done; trigger is BLOCKED until a remote + secrets exist | — |
| P1-18 | Headers | CSP/header tightening + automated header test | PASS | `next.config.ts`, `scripts/test-headers.mjs` | `npm run test:headers` | 13/13. Removed `unsafe-eval` (verified via real prod build + Playwright signup flow, zero CSP violations). Added `frame-ancestors 'self'` + HSTS. Removed stale `api.anthropic.com` connect-src entry (app calls OpenAI server-side only). | — | done | pending |
| P1-19 | Privacy | Account deletion route + UI if missing | PASS | Same as P0-08 | `npm run test:deletion` | Same as P0-08 | — | done | pending |
| P1-20 | Privacy | DATA_FLOW.md / RETENTION_POLICY.md / PRIVACY_OPERATIONS.md | PASS | `security/PRIVACY_RETENTION.md`, `scripts/cleanup-test-accounts.mjs` | manual | Accurate against the live 19-table schema. Found 107 leftover `*@example.com` test accounts from this session's test scripts; user approved cleanup, ran `scripts/cleanup-test-accounts.mjs` (real `auth.admin.deleteUser()`), `profiles` confirmed back to genuine rows only (108 -> 3, then +4 retried). | — | done | pending |

## P2 — Medium (load testing, accessibility, visual QA, docs) — only after P0/P1 substantially done

| ID | Release area | Exact task | Status | Implementation files | Verification command | Evidence | Blocker | Next action | Commit |
|---|---|---|---|---|---|---|---|---|---|
| P2-01 | Load | Load test scripts against local dev | PASS | `scripts/load-test.mjs` | `npm run test:load` | No new dependency — plain concurrent fetch + percentiles. Real prod-build run at concurrency=50: static pages 30-65ms p50-p99, zero errors/900 requests. /api/health slower (p95 775ms, real DB round-trip every call) — noted honestly, not a problem at normal polling intervals. | — | done | pending |
| P2-02 | E2E | Full 25-step paid-path Playwright run | PASS (honest partial scope) | `scripts/test-e2e-paid-path.mjs` | `npm run test:e2e-paid` | 6/6 for real signup -> billing -> genuine Stripe Checkout session (correct product/price) -> test-card field entry -> portal route health. Does NOT complete actual payment: investigated directly, concluded Stripe's bot/fraud detection blocks headless automated submission (appropriate, not a bug, not something to defeat). Webhook->entitlement mechanism already proven via real signed events (test-stripe-webhook.mjs) + earlier manual verification this session (P0-STRIPE-ENTITLEMENT). | Full UI confirmation of post-payment state needs one human-driven manual run | done for automatable scope | pending |
| P2-03 | Accessibility | Axe/Lighthouse pass on primary screens | PASS (fully) | `scripts/test-accessibility.mjs`, all 6 public pages | `npm run test:accessibility` | 6/6 pages at ZERO violations of any severity. Normalized remaining text-muted-foreground opacity on waitlist (29 contrast nodes -> 0). Added real `<main>` landmarks to all 6 public pages (waitlist, login, signup, pricing, privacy, terms). | — | done | pending |
| P2-04 | Visual QA | Desktop+mobile screenshot review, all primary screens | PASS | `scripts/capture-screenshots.mjs`, `next.config.ts`, privacy/homepage/resume pages, README/SECURITY/AI_SETUP.md | `npm run capture:screenshots` then visual review | Reviewed 12 real screenshots. Found+fixed: (1) dev-mode CSP had no eval() carve-out, causing a visible Next dev-tools error badge — made script-src conditionally allow unsafe-eval only in dev, re-verified prod stays strict. (2) Privacy policy + homepage + in-app resume page told users their resume is processed by "Anthropic's Claude API" — factually wrong (app uses OpenAI), a real privacy-policy accuracy bug, not cosmetic. Fixed all 3 plus stale docs (AI_SETUP.md was the likely source, fully rewritten). | — | done | pending |

## PQ — Product Quality (2026-06-20 mega-prompt: full product transformation pass)

Context: user gave a sweeping 22-part product/design/security/ops mandate to make Showcase
"as complete, polished, secure, reliable, cohesive, commercially viable, and
production-operable as current engineering practices realistically allow." That scope is
multi-week for a real team; this section tracks real, evidence-based progress against it
incrementally, in priority order, without re-litigating whether the full scope fits one
session (it doesn't — see notes below).

Survey done first (via Explore agent, then independently spot-checked — one claim in the
survey turned out wrong and was corrected before acting): mapped real vs. demo/shallow depth
across resume parsing, ProofScore, portfolio builder, jobs (browse/for-you/pipeline), Tailor
Studio, Truth Ledger, ATS export, billing/quotas, onboarding. Most flows are genuinely real
and wired end-to-end. Two concrete gaps were found and prioritized:

| ID | Area | Exact task | Status | Implementation files | Verification | Evidence | Next action | Commit |
|---|---|---|---|---|---|---|---|---|
| PQ-01 | ProofScore | Deterministic scoring engine (AI was 100% determining scores, violating the explicit "AI must never determine deterministic ProofScore components" rule) | PASS | `src/lib/proofscore/engine.ts` (new), `src/lib/ai/schemas.ts`, `src/lib/ai/prompts.ts` (buildAuditExplanationPrompt replaces buildAuditPrompt), `src/app/api/ai/audit-portfolio/route.ts`, `src/types/database.ts`, `src/app/(app)/audit/page.tsx` | `npm run test:proofscore` (13/13, new) + real E2E through the actual signup->resume-parse->audit UI flow (not a script) | Built a real deterministic engine: 11 fixed categories (role positioning, first-impression clarity, target-role alignment, evidence strength, quantified impact, project depth, case-study quality, credibility signals, contact readiness, keyword support, presentation clarity), fixed weights summing to 100, every score computed from countable facts in parsed resume/portfolio JSON (bullet metric counts, link presence, keyword overlap, etc.) — same input always produces the same score (proven by test). AI is now given the precomputed score + evidence per category and asked ONLY to explain it in plain language; its response schema has no score field at all, so it structurally cannot determine a score. Verified live: real signup -> real resume paste -> real analyze-resume parse -> real audit run through the actual UI, screenshot confirms correct 94/100 render with category-level evidence-grounded explanations (e.g. correctly identified the one vague bullet and explained exactly why evidence_strength scored 80/100). Found and fixed a related real bug while in this code: the frontend's `AuditCategory` type claimed `score: number` (non-nullable) when it was always actually `number \| null` for free-tier-locked categories — this silently hid a real rendering bug (`null%` width bars, `cat.score >= 80` always false-but-uncaught) from TypeScript. Fixed the type to be honest and added a real "Upgrade to Pro" locked-category UI state instead of rendering broken null math. | Could extend keyword_support to use a real per-role-family expected-skills database instead of simple word-overlap, if a future session has bandwidth — not release-blocking, current version is honest and evidence-based even if simpler. | pending |
| PQ-02 | Jobs | Fixture/demo job data must be labeled, never silently mixed with live data | NOT_NEEDED (already correct) | `src/app/(app)/jobs/page.tsx` | code read | Survey initially flagged this as a gap; on inspection it was already correctly implemented — per-listing "Demo listing" badge (line ~409) and a page-level "Demo listings" badge (line ~850) both exist and key off `job.provider === 'fixture'` / `is_demo`. No change needed. Recorded here so a future session doesn't re-investigate the same already-closed question. | — | n/a |
| PQ-03 | Security | SSRF — never fetch arbitrary user-supplied URLs without a hardened policy | PASS (N/A — no such feature exists) | n/a | `grep -rn "await fetch(" src/app src/lib` | Audited every server-side `fetch()` call in the codebase (5 total: external job provider, jobdataapi provider, error-reporter webhook, distributed rate-limiter pipeline). All 5 use a server-configured URL (env var), never a user-supplied one. `jobs/import`'s `source_url` field is stored as plain text metadata for the user's own reference link — it is never fetched server-side. There is currently no feature (URL preview, og:image fetch, link validation, etc.) that fetches a URL a user controls, so there is no SSRF surface to harden. Documented rather than building unused hardening infrastructure for a feature that doesn't exist — if a URL-fetching feature is added later, it must go through a hardened policy (deny private/loopback/link-local ranges, enforce scheme allowlist, no redirects to internal hosts) at that time. | — | n/a |
| PQ-04 | Reliability | Separate liveness vs. readiness endpoints | NOT_NEEDED (platform mismatch) | `src/app/api/health/route.ts` | code read | Part 16 of the mega-prompt asks for separate liveness/readiness/health endpoints, which is a Kubernetes-shaped concept (is the process alive vs. is it ready to serve traffic). This app deploys to Vercel serverless functions, which have no persistent process to probe for liveness — every invocation is a fresh cold-or-warm start, so a "liveness" check would always return 200 if it runs at all, making it a no-op endpoint that exists only to satisfy a checklist item. The existing `/api/health` already does the one check that's actually meaningful on this platform: real DB connectivity + config presence (readiness-equivalent). Decided not to add a placeholder liveness endpoint with no real behavior — that would be exactly the kind of filler the user's own standing instructions warn against. Revisit only if the app ever moves to a long-lived-process host. | — | n/a |
| PQ-05 | Portfolio | Distinct, real theme variants (Executive Dark / Clean Editorial / Creative Case Study) — `portfolios.theme` column already exists in the schema but nothing reads it; the public portfolio page (`src/app/p/[slug]/page.tsx`, 591 lines) always renders one hardcoded layout regardless of the stored theme value | NOT_STARTED | `src/app/p/[slug]/page.tsx`, `src/app/(app)/builder/page.tsx` | none yet | **This is the next task to pick up.** Scope: (1) extract the current 591-line layout as the first theme ("Executive Dark" — it already matches that description: dark background, violet glow, bold), (2) build at least one genuinely distinct second theme ("Clean Editorial" — light background, serif headlines, magazine-style single-column layout) as a real alternate renderer, not a recolor, (3) wire `portfolio.theme` through the builder's theme selector (check if one exists — `grep -n theme src/app/(app)/builder/page.tsx`, currently empty per this session's check, so the builder UI for picking a theme likely needs building too) and the public page's render-switch, (4) preserve accessibility (axe-clean), responsiveness, and print behavior on every theme — re-run `npm run test:accessibility` won't cover `/p/[slug]` automatically since it needs a published portfolio fixture; extend the script's page list or write a focused check, (5) a third theme ("Creative Case Study") only if time remains after 1+2 are solid — two genuinely distinct, polished themes beat three rushed ones. | Needs a real published portfolio (real signup -> resume -> generate-portfolio -> publish) to test against, not a demo route. | start here |
| PQ-06 | Onboarding | User-reported (screenshot): onboarding's resume step was paste-only text, no file upload — and, found while investigating, the resume row it created was never sent through analyze-resume, so it had `raw_text` but no `parsed_json` at all | PASS | `src/components/shared/file-upload-zone.tsx` (new, extracted from `src/app/(app)/resume/page.tsx` for reuse), `src/app/(app)/resume/page.tsx`, `src/app/(app)/onboarding/page.tsx` | Real E2E through the actual UI (not a script): signup -> onboarding steps 0/1 -> real `.txt` file upload at step 2 -> Go to dashboard -> queried Supabase directly | Extracted the resume page's existing real upload component (drag-drop, PDF/DOCX/TXT, magic-byte-adjacent extension+MIME checks, 4MB limit, server-side extraction via `/api/resume/extract-text`, best-effort storage archival) into a shared component and added it to onboarding alongside the paste fallback. Fixed the deeper bug: onboarding's `finish()` now calls `/api/ai/analyze-resume` with the new resume's id right after inserting it, exactly matching the real Resume page's two-step pattern — verified by direct DB query after a real run: `parsed_json` populated with correctly-extracted name/skills, not just `raw_text`. This bug would have hit every new user who filled in the onboarding resume step: their first ProofScore run or portfolio generation attempt would have dead-ended asking them to "parse it first," directly undermining the product's own stated requirement that onboarding must lead to an immediate first result. | — | pending |
| PQ-07 | Resume upload | User-reported (screenshot): a real resume.pdf hit "Could not read this file" with no working fallback — user demanded extraction "can never fail to read the file" | PASS | `src/lib/ai/pdf-vision.ts` (new), `src/app/api/resume/extract-text/route.ts` | Reproduced the exact failure class first (image-only PDF via `sips` PNG->PDF, no embedded text layer — confirmed `pdf-parse` returns near-empty text, length 16), then verified the fix rescues it through the real authenticated UI, not just a script. | Root cause: `pdf-parse`/`pdfjs-dist` can only extract text that has an actual embedded text layer with Unicode mapping. Scanned/photographed resumes and PDFs exported with outlined fonts (some design-tool exports) have neither — no text-extraction library, however well-implemented, can read them, because the data genuinely isn't text. Added a real fallback, not a better error message: when standard extraction throws OR returns under 50 chars, the route now sends the raw PDF directly to OpenAI's vision-capable model (`responses.create` with `input_file`, base64, no separate file upload step needed) and asks for verbatim transcription. Verified end-to-end: the previously-failing image-only PDF now extracts its full, correct text through the real signup -> resume-upload UI flow. Verified this doesn't introduce false positives either — the existing adversarial `test:uploads` suite's "real %PDF-signed but unparseable PDF" case (garbage bytes, no actual content) still correctly falls through to the honest `extraction_too_short` error after the vision attempt also (correctly) finds nothing, rather than hallucinating content. Scope: this fallback is PDF-only (the demonstrated failure class); DOCX failures remain on `mammoth`'s existing (already robust) path, not changed. Follow-up: added a dedicated `resume_pdf_vision` rate limit (2/day free, 10/day Pro) since this path calls a paid model and `extract-text` previously had no rate limiting at all — a limit hit only skips the fallback, never blocks normal free text extraction. | — | pending |
| PQ-08 | Portfolio generation | User feedback (blunt, paraphrased): the generated portfolio "is one of the most shit portfolios I've ever seen," and onboarding asks for too much input that the system should already have. | PASS | `supabase/migrations/012_profile_goal_and_links.sql`, `src/app/(app)/onboarding/page.tsx`, `src/app/(app)/builder/[portfolioId]/page.tsx`, `src/lib/ai/prompts.ts`, `src/lib/constants.ts` | Real E2E through the actual UI: onboarding with real goal+links+resume -> direct DB check (profile fields correctly persisted, portfolio row created) -> real Pro-gated generation call -> inspected actual JSON output and rendered public page screenshots, not just "did the API return 200." | Root cause was data-plumbing, not a content-quality ceiling: onboarding's "Goals & Links" step collected portfolio_goal/linkedin/github/website and then discarded all of it — `profiles` had no columns for them. Generation was then called with industry hardcoded to "Technology", portfolioGoal hardcoded to "Active job search", and links hardcoded to `{}` regardless of what the user actually entered. Fixed by adding the missing columns, actually saving them, and passing real values into generation. Also stopped re-parsing the resume from scratch on every generation call (now reuses stored parsed_json — faster, doesn't pay for the same AI call twice). While reviewing the actual generated output critically (not just confirming fields were non-empty), found and fixed two further fabrication-adjacent bugs: (1) the recruiter-summary prompt's own example text invented a work-mode/location preference with zero source basis — removed; (2) resumes with no formal "Projects" section produced zero case studies, leaving the public page with no evidence-driven centerpiece at all — added an explicit instruction to elevate the strongest experience achievement into a real Problem/Process/Outcome case study when none exists, with a tightening pass so the "process" field can't borrow an unstated method from the candidate's general skills list. Verified via real before/after screenshots of the same portfolio: before had zero case studies and a fabricated "prefers remote" claim; after has a real sourced case study and an honest recruiter summary. | Did not attempt full auto-trigger-generation-with-zero-clicks (would require either blocking onboarding completion on a second long AI call, or a separate "first visit to builder" auto-trigger) — the manual-input reduction delivered here is the redundant-re-entry elimination, not zero-click generation. Worth a follow-up if more "minimal effort" complaints surface. | pending |
| PQ-09 | Builder autosave + public page visual design | User feedback (blunt, paraphrased): "visually it was shit like its so plain and boring i hate it." | PASS | `src/app/(app)/builder/[portfolioId]/page.tsx`, `src/app/p/[slug]/page.tsx`, `src/components/shared/reveal.tsx` (new) | Generated a portfolio with zero manual Save clicks and confirmed via direct DB query that the full generated JSON (not an empty object) was persisted automatically; visually verified the redesign via real Playwright screenshots of a published portfolio at multiple scroll depths, not just a code read. | While building the screenshot-verification harness for the visual fix, discovered the actual bug was upstream of styling: clicking Generate and trusting autosave (the obvious user path) silently lost the output. `scheduleAutosave()` was called manually from inside each setState updater and captured a `save` closure from *before* that exact edit was applied — every isolated edit's autosave wrote back stale, pre-edit content. A second edit before the timer fired would mask it (closure "catches up"), and the explicit Save button never hit it (always fresh on click) — so it survived undetected through all prior manual QA. Generate-then-walk-away is exactly the one common path that always triggered it. Fixed by replacing the manual scheduling with a `useEffect` keyed on `[title, targetRole, content]`, which always closes over the value just committed. Separately, the public portfolio page's visual flatness was real: ambient lighting stopped at the hero fold leaving every section below as a dead flat wall, every section repeated an identical label treatment with zero motion, and the skills section was a sparse text-column list that looked broken on thin resumes. Extended the ambient background across the full scroll height, added large faint section-numeral watermarks (00-03) for visual rhythm, added scroll-triggered reveal animations via a small new IntersectionObserver client component (respects prefers-reduced-motion), and rebuilt skills as a colored pill cloud. | The autosave bug likely affected every prior builder session, not just generation — any user who made one edit and trusted autosave without a subsequent edit or explicit Save click may have lost that edit. No backfill possible (the bad saves already overwrote the old content in place; there's no separate "intended" version to recover). No data-loss notification was sent — flagging here for awareness rather than as an unresolved item, since the fix is forward-looking only. | pending |

## Release gate

See `security/release-gate.json` (machine-readable) and run `npm run release:gate`.

## Notes for future sessions

- Do not repeat Phase-1 regression baseline unless `git log` shows changes since `2e9ba51` that touched test-relevant files.
- Do not re-litigate whether the original mega-audit prompt's full scope is achievable in one session — it isn't, and that's already been said. Just work the manifest.
- BLOCKED rows have an exact one-line human action each — do not attempt to work around them (no fabricated accounts, no guessed credentials, no skipping verification).
